import styled from "styled-components";

export default styled.div`
        font-family: adage-script-jf, sans-serif;
        padding-right: 20px;
        font-weight: 900;

        a {
            color: darkred;
            font-size: 2vw;
        }

        a:hover {
            color: darkorange;
        }

`;